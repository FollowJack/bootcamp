/* global describe, test */
describe('DataManager', () => {
  test('gets start timestamp', () => {
  })
  test('sends player movement', () => {
  })
  test('gets aggregated player movements', () => {
  })
})
